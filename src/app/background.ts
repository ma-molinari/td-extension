chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
    // console.log("Background got a message!")
})

//functions

// chrome.runtime.reload()

// chrome.runtime.restart()

// chrome.runtime.restartAfterDelay(integer seconds, function callback)

// $(".symbol-17NLytxZ").bind("DOMSubtreeModified", function(e) {
//     if (e.currentTarget.dataset.symbolFull === 'BMFBOVESPA:WDON2020') {
//         console.log(e.currentTarget.children[2].children[0].textContent)
//     }
//  });

// var snd = new Audio(foom);
// snd.play();